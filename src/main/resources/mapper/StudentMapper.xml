<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.studentmanagement.mapper.StudentMapper">
    
    <resultMap id="BaseResultMap" type="com.example.studentmanagement.entity.Student">
        <id column="id" property="id"/>
        <result column="student_no" property="studentNo"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="age" property="age"/>
        <result column="class_name" property="className"/>
        <result column="major" property="major"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="address" property="address"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <sql id="BaseColumnList">
        id, student_no, name, gender, age, class_name, major, phone, email, address, created_at, updated_at
    </sql>
    
    <insert id="insert" parameterType="com.example.studentmanagement.entity.Student">
        INSERT INTO student (student_no, name, gender, age, class_name, major, phone, email, address)
        VALUES (#{studentNo}, #{name}, #{gender}, #{age}, #{className}, #{major}, #{phone}, #{email}, #{address})
    </insert>
    
    <update id="update" parameterType="com.example.studentmanagement.entity.Student">
        UPDATE student
        <set>
            <if test="studentNo != null">student_no = #{studentNo},</if>
            <if test="name != null">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="age != null">age = #{age},</if>
            <if test="className != null">class_name = #{className},</if>
            <if test="major != null">major = #{major},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="address != null">address = #{address},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM student WHERE id = #{id}
    </delete>
    
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/> FROM student WHERE id = #{id}
    </select>
    
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/> FROM student ORDER BY created_at DESC
    </select>
    
    <select id="selectByQuery" parameterType="com.example.studentmanagement.entity.StudentQuery" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/> FROM student
        <where>
            <if test="studentNo != null and studentNo != ''">AND student_no LIKE CONCAT('%', #{studentNo}, '%')</if>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="gender != null">AND gender = #{gender}</if>
            <if test="className != null and className != ''">AND class_name LIKE CONCAT('%', #{className}, '%')</if>
            <if test="major != null and major != ''">AND major LIKE CONCAT('%', #{major}, '%')</if>
        </where>
        ORDER BY created_at DESC
        <if test="pageNum != null and pageSize != null">
            LIMIT #{pageSize} OFFSET #{pageNum * pageSize}
        </if>
    </select>
    
    <select id="countByQuery" parameterType="com.example.studentmanagement.entity.StudentQuery" resultType="java.lang.Long">
        SELECT COUNT(*) FROM student
        <where>
            <if test="studentNo != null and studentNo != ''">AND student_no LIKE CONCAT('%', #{studentNo}, '%')</if>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="gender != null">AND gender = #{gender}</if>
            <if test="className != null and className != ''">AND class_name LIKE CONCAT('%', #{className}, '%')</if>
            <if test="major != null and major != ''">AND major LIKE CONCAT('%', #{major}, '%')</if>
        </where>
    </select>
    
    <select id="selectByStudentNo" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/> FROM student WHERE student_no = #{studentNo}
    </select>
</mapper>
